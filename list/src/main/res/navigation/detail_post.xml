<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/detail_post"
    app:startDestination="@id/listActivity">

    <activity
        android:id="@+id/listActivity"
        android:name="com.idealista.android.challenge.list.ui.ListActivity"
        android:label="activity_list"
        tools:layout="@layout/activity_list" />

    <activity
        android:id="@+id/detailActivity"
        android:name="com.idealista.android.challenge.list.ui.DetailActivity"
        android:label="activity_detail"
        tools:layout="@layout/activity_detail" />
    <action
        android:id="@+id/action_global_detailActivity"
        app:destination="@id/detailActivity" />
</navigation>